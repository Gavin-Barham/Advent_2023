{
  "name": "ts",
  "version": "1.0.0",
  "description": "advent of code ts",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "clear": "rm -rf ./dist",
    "start": "node ./dist/index.js",
    "dev": "npm run clear && npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.4"
  }
}
